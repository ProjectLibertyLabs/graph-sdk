[workspace]
members = [
    "core",
    "config",
    "bridge/ffi",
    "bridge/jni",
    "log-result-proc-macro",
    "bridge/node",
]

default-members = [
    "core",
    "config",
    "bridge/ffi",
    "bridge/jni",
    "log-result-proc-macro",
]

[profile.release]
panic = "unwind"

[profile.production]
inherits = "release"
lto = true
codegen-units = 1
strip = true
