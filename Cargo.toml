[workspace]
members = [
    "core",
    "config"
]

default-members = [
    "core",
    "config"
]

[profile.release]
panic = "unwind"

[profile.production]
inherits = "release"
lto = true
codegen-units = 1
strip = true
