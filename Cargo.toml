[workspace]
members = [
    "core",
    "config",
    "bridge/ffi",
    "bridge/jni",
]

default-members = [
    "core",
    "config",
    "bridge/ffi",
    "bridge/jni",
]

[profile.release]
panic = "unwind"

[profile.production]
inherits = "release"
lto = true
codegen-units = 1
strip = true
